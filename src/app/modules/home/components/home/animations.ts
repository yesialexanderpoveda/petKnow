import{
animate,
query, 
sequence, 
stagger,
style,
state,
transition,
trigger,
keyframes
} from "@angular/animations"

export const animationHeader = trigger('openClosed', [

  //....

  state('', style({

   
  })), 

  state('closed', style({
  
  
   
  })),

  
  


])